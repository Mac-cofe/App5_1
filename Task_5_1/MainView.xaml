<Window x:Class="App5_1.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:App5_1"
             mc:Ignorable="d" 
             Height="90" Width="330"
        Title="Задание. Создание кнопок" WindowStartupLocation="Manual" ResizeMode="NoResize">
    <StackPanel Orientation="Horizontal" Background="Gray">
        <Button Content="Количество труб" Command="{Binding SelectCommand1}" 
                Margin="4" Padding="3" Height="50"/>
        <Button Content="Объем стен" Command="{Binding SelectCommand2}" 
                Margin="4" Padding="3" Height="50"/>
        <Button Content="Количество дверей" Command="{Binding SelectCommand3}" 
                Margin="4" Padding="3" Height="50"/>
    </StackPanel>
</Window>
